import 'package:flutter/material.dart';

class ProviderMessageScreen extends StatelessWidget {
  const ProviderMessageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("ProviderMessageScreen"),
      ),
    );
  }
}
